// import React from 'react';
// import PropTypes from 'prop-types';
import { Button, StyledLink as SL, ButtonRow as BR } from './button.theme';

export const ButtonRow = BR;
export const ButtonLink = SL;
export default Button; // (props) => <Button {...props} />;
