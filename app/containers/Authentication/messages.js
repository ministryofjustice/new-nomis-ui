/*
 * Authentication Messages
 *
 * This contains all the text for the Authentication feature.
 */
import { defineMessages } from 'react-intl'

export default defineMessages({
  authFailed: {
    id: 'app.components.Authentication.failed',
    defaultMessage: 'The username or password you have entered is invalid.',
  },
})
