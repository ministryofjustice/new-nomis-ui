import styled from 'styled-components';

export const Main = styled.div`
  display: flex;
  overflow: hidden;
    flex-wrap: wrap;
    position: absolute;
    height: calc(100% - 200px);
    width: 100%;
    top: 0;
    left: 0;
`;
