

describe('application', () => {
  it('should add the token information to the current web session on a successful login');
  it('should request a new token when the existing one has expired');
  it('should return a 401 once the web session has timed out');

});